# Existing intranet PostgreSQL 12+ connection string. Do not commit the real value.
DATABASE_URL=postgres://collector:change-me@postgres.internal.example:5432/yibiao_analytics
PORT=8787
HOST=127.0.0.1
# Set true only when a trusted reverse proxy supplies the client address.
TRUST_PROXY=false
