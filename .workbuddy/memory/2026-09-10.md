- 09-10：step03 目录生成页 UI 优化需求确认（标书原文多处改按块堆叠展示+第一处/第二处标题+类型徽标；三分屏支持拖拽分割线调宽+每屏右上角隐藏按钮+顶部切换条恢复，至少保留一屏），已产出可交互效果图 docs/step03-outline-ui-mockup.html 待小哥确认后落地代码（涉及 OutlineEditPage.tsx / TenderSourcePanel.tsx / feature-technical-plan.css，现布局为 repeat(3,1fr) 固定等宽 grid）
- 09-10 17:0x：step03 目录生成页 UI 优化代码落地——标书原文面板按块堆叠（每块"第N处"标题+类型徽标+关键词高亮，TenderSourcePanel.tsx 的 viewModel.items 整列展示，移除外置左右箭头切换）；三分屏 grid 改 flex 实现可拖拽分割线调宽（pointer 事件 + flexBasis，双击分割线恢复等宽）+ 顶部 outline-workspace-restore-bar 切换条（高亮=显示中，点击 toggle，至少保留一屏）+ 每屏右上角 outline-pane-hide-action 隐藏按钮（悬停浮现）；紧凑模式（@container max-width 899px）下隐藏分割线/切换条/隐藏按钮，仅保留原 tab 切换。改动落到 OutlineEditPage.tsx / TenderSourcePanel.tsx / feature-technical-plan.css，同步修 workflowLayout.test.ts 中过期的 activeMarkdown 断言匹配新的 itemMarkdown 渲染。验证：node --test 全 59 项通过，npm run build ✓（built in 11.58s）。
- 09-10 17:25：step03 目录生成页 bug 修复——之前把 tree 分屏的 hide button 放在 .outline-tree-tools 容器内，被 .outline-tree-tools button 规则（padding 7px 10px + 蓝色椭圆 pill 样式 + flex: 1 1 100% 换行）覆盖成视觉上无法识别的图标按钮，且 hover 后也无样式区分。两处修复：1) JSX 把 tree 的 hide button 移出 tree-tools 容器，作为 .outline-tree-head 的并列子节点（与 title/tools 平级），跟 source/detail 一致放最右；2) CSS 把 .outline-tree-tools button 改成 :not(.outline-pane-hide-action)，并把 .outline-tree-tools 的 flex 限定为 0 0 auto 让其不再 100% 换行。验证：node --test 31 项全过，npm run build ✓。
- 09-10 17:30：step03 显隐 bug 根因——workspaceShellRef 在 428 行声明但从未绑到 <div className="outline-workspace-shell">，导致 toggleWorkspacePane 调用 resetWorkspacePaneWidths 时 workspaceShellRef.current 为 null、内联 flexBasis 静默无法清除。后果：用户拖过分割线后，每个 pane 都带 style="flex-basis: 360px" 这种内联样式；hide 一个 pane 后，className 加上 .is-hidden，但内联 style 优先级高于 className，于是"隐藏"失效（视觉上仍按拖过的宽度显示）。两处修复：1) JSX 给 .outline-workspace-shell 加 ref={workspaceShellRef} 让 resetWorkspacePaneWidths 真正生效；2) CSS 给 .outline-workspace-shell > .outline-generation-workspace > [role="tabpanel"].is-hidden 关键属性加 !important（flex / flex-basis / width / min-width / padding / overflow），即使内联 style 残留也强制 width:0 隐藏。验证：node --test 31 项全过、npm run build ✓。
- 09-10 17:35：step03 拖拽分割线优化——之前 startWorkspaceResize.handleMove 只对 leftPane/rightPane 写 inline flexBasis，未锁住第三个 panel。三 panel 同为 flex: 1 1 0，被写入 flexBasis 后容器按 (grow, basis, shrink) 重新分配，两个 divider 之一被拖时第三个 panel 也会被动调整，导致"拖动任一分割线对侧宽度突变"。修复：在 pointerdown 时遍历所有 [data-pane]，对非 leftPane/rightPane 的可见 pane 读取 getBoundingClientRect().width 并写入 inline flexBasis = 初始宽度，把它们锁住；handleMove 只改 leftPane/rightPane 两个目标。验证：node --test 31 项全过、npm run build ✓。
- 09-10 17:40：step03 拖拽"闪一下"根因——之前仅 inline style.flexBasis = 'Npx' 不能真正锁定宽度：flex 容器中第三 pane 仍带 flex: 1 1 0，flex-grow: 1 仍生效。pointerdown 那一帧 sum basis = 0(source) + 0(tree) + 396(detail inline) = 396 < container 1188，剩余 792 被 grow 1:1:1 分配，每份 264，第三 pane 实际宽度 = 396 + 264 = 660px → "闪一下就变"。修复：pointerdown 锁第三 pane 时同时写 flexGrow='0' + flexShrink='0' + flexBasis='Npx'（等价 flex: 0 0 Npx），让它不参与 grow/shrink 重新分配。数学验证：sum basis = 406+386+396 = 1188 = container → remaining 0，detail 实际宽 = 396 锁定。验证：node --test 31 项全过、npm run build ✓。
- 09-10 17:50：step03 拖拽后 pane 坍缩到 0 根因——之前 startWorkspaceResize 只锁第三 pane（写 flexGrow=0/flexShrink=0/flexBasis=Npx），但 resetWorkspacePaneWidths 只清 flexBasis 不清 flexGrow/flexShrink。用户拖拽完一次，第三 pane 带 inline flexGrow=0/flexShrink=0，后续 toggleWorkspacePane 调 resetWorkspacePaneWidths 仅清 flexBasis，flexGrow/flexShrink 仍残留 inline，叠加 CSS 默认 flex:1 1 0 被覆盖成 flex:0 0 0 → pane 宽度=0。修复方案：1) startWorkspaceResize 改为只锁 leftPane 和 rightPane（写 flexGrow=0/flexShrink=0/flexBasis=Npx），不再碰第三 pane，让其保持 CSS 默认 flex:1 1 0 自动占据剩余空间（数学验证：sum basis = leftBasis + rightBasis + 0(third) = ... container - 这两个 = remaining，第三 pane grow=1 独得 remaining，宽度严格不变，不再闪一下）；2) resetWorkspacePaneWidths 同时清 flexGrow、flexShrink、flexBasis 三个属性。验证：node --test 31 项全过、npm run build ✓。
