# 2026-09-11

## 标书生成流程优化方案讨论（STEP 01/02 合并评估）

- 小哥提出把标书生成第一步（选择标书）与第二步（招标文件解析）合并。经代码评估（DocumentAnalysisPage / BidAnalysisPage / technicalPlanStore / taskService / bidSectionDetector），结论：不建议物理合并（步骤数 6->5 的状态机、恢复、埋点、文档代价大），建议"逻辑合并"——把标段判定前置到 STEP 01。
- 依据：`selectBidSection()` 会重建 tender.md 并 `clearDownstreamFromBidSectionChange()` 清空下游，判定越晚返工越大；`detectBidSections()`（bidSectionDetector.cjs）为纯本地正则、零 Token，上传即可跑。
- 小哥随后给出竞品"标书设置"页截图（多标段/投标公司/标书篇幅阶梯/写作风格/风格预设/表格/图片/模板/防重检测+DeepSeek 切言）。代码验证：约半数配置项不存在（投标公司库、写作风格、风格预设、防重检测、智能切言），已有的分散在 STEP 02（投标范围）、STEP 03（OutlineWordControlOptions 字数控制）、STEP 05（ContentGenerationOptions：tableRequirement + AI/Mermaid/HTML 图片开关）。
- 小哥确认：截图为竞品参考不照抄；接受"STEP 01 早选、后续步骤仍可改"语义。已按方案 X（STEP 01 快速配置条，4 项：投标范围/标书篇幅/表格密度/图片开关）产出 spec：docs/superpowers/specs/2026-09-11-step01-quick-bid-config-design.md，待小哥确认后进入实现。
- 关键 IPC 已验证可复用：saveOutlineConfig、saveContentGenerationOptions、checkBidSections、selectBidSection、startBidSectionExtraction。
- 已产出 STEP 01 改造交互原型：docs/design-preview/step01-quick-config-mockup.html（沿用项目原型规范：--yb-* token + 顶部说明条，支持多标段/单标段形态切换、标段选择对话框、页数阶梯/表格密度/图片开关交互），待小哥确认。
