# 易标投标业务语境

本词汇表统一招标文件分析与技术方案编制中的领域语言，避免评分表分类与投标文件编排要求相互混淆。

## 评分分类

**评分大类**:
招标文件评分表明确列出的一级分类，例如技术方案、综合实力、商务或价格；原文存在明确分类时，它是评分归属的权威边界。
_Avoid_: 评分分组、评分维度

**技术评分项**:
明确归属于技术类评分大类的具体评分条目；仅当原文没有明确评分大类时，才按是否要求展开具体技术方案判定。
_Avoid_: 技术文件内容、技术要求

**评分大项**:
用于技术目录规划和分支映射的最小评分组织单元。原文存在明确业务分组时，评分大项对应该业务分组；原文没有明确业务分组时，评分大项对应一个独立技术评分项。它对应 `technical-score-groups.json.groups` 和 `score-directory-plan.json` 中的 `requirement_id`。
_Avoid_: 评分行、评分维度、评价指标

**评分行**:
评分大项下的一条独立评分标准或评分要求，保存在评分大项的 `criteria` 中。评分行用于生成评分大项以下的目录和覆盖记录，不直接作为 `score-directory-plan.json` 的目录映射单位。
_Avoid_: 评分大项、评分维度

**明确响应点**:
评分行中要求投标文件明确回应的具体内容，保存在 `criteria.response_points` 中，用于生成更下级目录或正文编写要求。
_Avoid_: 评分行、评价维度

**评分规划映射**:
评分大项与技术目录节点之间的映射关系。`score-directory-plan.json` 的 `mappings` 只映射评分大项，不直接映射评分行或明确响应点。
_Avoid_: 评分行映射、正文小节映射

**综合实力评分项**:
明确归属于综合实力评分大类的评分条目，不因其证明材料需要编入技术文件而转为技术评分项；在完整投标文件结构中仍按原结构保留。
_Avoid_: 技术评分项、技术方案评分项

**商务评分项**:
归属于综合实力、商务或资信等非技术评分大类的评分条目；它与技术评分项分别提取，不因技术评分分析而自动生成或迁移。
_Avoid_: 技术评分项、自动转入项

**技术评分排除项**:
在技术文件独立成册模式中，模型曾作为技术评分候选返回、但依据原文评分大类被确定为非技术评分的条目；它不会进入独立技术目录候选。
_Avoid_: 解析失败项、已删除评分项

## 投标文件结构模式

**技术文件独立成册**:
只以技术评分项组织技术文件一级目录的编制模式，不承载商务、资信、价格或综合实力等非技术评分结构。
_Avoid_: 完整投标文件、全套投标目录

**完整投标文件结构**:
按招标文件要求保留通用、商务、资信、技术及其他组成部分的编制模式，不因独立技术文件的评分过滤规则裁减原有结构。
_Avoid_: 独立技术文件、纯技术目录

## 流程统计

**流程操作**:
用户在标书项目中发起、由系统实际执行并可确定终态的业务步骤，包括新建标书、标书解析、目录生成、正文生成和 Word 导出。流程操作覆盖技术方案与已有方案扩写，并以流程类型区分。
_Avoid_: 页面访问、按钮点击、任务日志

**流程事件**:
对单次流程操作生命周期的统计记录。每个流程操作分别记录 `started`、`succeeded`、`failed` 或 `cancelled` 终态，用于计算完成率和耗时；它不是业务正文或任务日志的副本。
_Avoid_: 任务日志、错误诊断包、普通页面埋点

**流程操作标识**:
一次流程操作发起时生成的随机 UUID。它把同一次操作的 `started` 事件与其唯一终态关联起来，并区分同一标书项目的重复、重试或并行操作。
_Avoid_: 标书项目 ID、任务标题、页面实例 ID

**流程项目元数据**:
流程事件中随事件快照保存的标书项目 ID 和项目名称。解析事件额外保存输入招标文件的文件名列表，导出事件额外保存导出文件名；不保存路径、文件内容或正文。
_Avoid_: 文件路径、文件正文、任务参数

**失败码**:
流程操作失败或取消时记录的受控类别，例如 `network`、`parse`、`ai`、`export` 或 `cancelled`。失败码不包含原始错误文字。
_Avoid_: 错误栈、模型响应、错误原文

**流程事件保留策略**:
内网流程事件永久保留。由于事件包含标书项目 ID、项目名称和文件名，流程事件表及其备份不得暴露到非内网环境。
_Avoid_: 临时诊断日志保留策略、公开埋点保留策略

**新建标书项目**:
以 `project_created` 表示的流程操作。项目记录创建并开始导入首批招标文件时进入 `started`，全部文件导入成功后才进入 `succeeded`。
_Avoid_: 打开文件选择器、创建空白页面、普通项目编辑

**标书导出**:
以 `word_export` 表示的流程操作。实际调用 Word 导出后进入 `started`，并根据本地导出结果进入 `succeeded`、`cancelled` 或 `failed`。
_Avoid_: 选择导出模板、打开已导出文件、导出进度日志
